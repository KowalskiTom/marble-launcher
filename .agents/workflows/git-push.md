---
description: Auto commit and push to remote repository without asking for permission
---

To commit and push without user intervention, follow these steps:

// turbo-all
1. Run `git add .`
2. Run `git commit -m "Updates"` (use a more descriptive message based on recent changes if possible)
3. Run `git push`
